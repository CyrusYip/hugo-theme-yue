{
  "name": "Yue",
  "version": "1.0.0",
  "scripts": {
    "server": "hugo server --navigateToChanged",
    "server:shared": "hugo server --navigateToChanged --bind 0.0.0.0",
    "server:production": "hugo server --navigateToChanged --environment production",
    "build": "hugo",
    "clean": "rm -r public/",
    "clean:server": "npm run clean; npm run server",
    "clean:server:shared": "npm run clean; npm run server:shared",
    "clean:server:production": "npm run clean; npm run server:production",
    "clean:build": "npm run clean; npm run build"
  },
  "hugo-bin": {
    "buildTags": "extended",
    "version": "0.128.1"
  },
  "devDependencies": {
    "hugo-bin": "^0.125.1"
  }
}
